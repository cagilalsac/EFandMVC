@model Role

<h4>Role</h4>
<hr />
<dl class="row">
    <dt class="col-2">
        @Html.DisplayNameFor(model => model.Name)
    </dt>
    <dd class="col-10">
        @Html.DisplayFor(model => model.Name)
    </dd>
    <dt class="col-2">
        @Html.DisplayNameFor(model => model.Users)
    </dt>
    <dd class="col-10">
        @* rol kullanıcılarının kullanıcı adına göre artan listelenmesi: *@
        @{
            string users = "";
            string seperator = "<br />";
            if (Model.Users is not null)
            {
                // 1. yöntem:
                /*
                foreach (var user in Model.Users.OrderBy(u => u.UserName))
                {
                users += user.UserName + seperator;
                }
                users = users.TrimEnd(seperator.ToCharArray()); // en sondaki <br /> temizlenmesi
                */
                // 2. yöntem:
                users = string.Join(seperator, Model.Users.OrderBy(u => u.UserName).Select(u => u.UserName));
            }
        }
        @users @* yukarıda atananan users değişken değerinin yazdırılması *@
    </dd>
</dl>